{:dev {:env
       {:database-url "postgresql://localhost/projectx_test?user=projectx&password=project"
        :jwt-secret "secret"}}

 :test {:env
        {:database-url "postgresql://localhost/projectx_test?user=projectx&password=project"
         :jwt-secret "secret"}}}
